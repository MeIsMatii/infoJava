public class Object extends ImprovedActor{

    // Interacting with a lever -> switch it
    // Interacting with an Item -> Take it
    public Object onInteract(Character trigger) {
        return null;
    }
}
